# i3status configuration file.
# see "man i3status" for documentation.

# It is important that this file is edited as UTF-8.
# The following line should contain a sharp s:
# ß
# If the above line is not correctly displayed, fix your editor first!

general {
    interval 		= 1
    colors 			= true
}

#order += "ipv6"
order += "disk /"
order += "ethernet eth0"
#order += "load"
#order += "cpu_temperature 0"
#order += "sysdata cpumem"
#order += "volume master"
order += "time"

ethernet eth0 {
        # if you use %speed, i3status requires root privileges
        format_up = " %ip"
        format_down = " down"
}


time {
        format = " %Y-%m-%d  %H:%M:%S"
}

#sysdata cpumem {
#	high_threshold = 90
#	med_threshold = 75
#	format = " {cpu_temp} {cpu_usage}% {mem_used_percent}"
#	on_click 1 = "exec xfce4-taskmanager"
#}


disk "/" {
        format = "%avail"
#	on_click 1 = "exec thunar /"
}

#volume master {
#        format = " %volume"
#        format_muted = " muted"
#        device = "default"
#        mixer = "Master"
#        mixer_idx = 0
#	on_click 1 = "exec xfce4-mixer"
#}
